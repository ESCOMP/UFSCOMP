<?xml version="1.0"?>

<compsets version="2.0">

  <help>
    =========================================
    compset naming convention
    =========================================
    The compset longname below has the specified order
    atm, lnd, ice, ocn, river, glc wave cesm-options

    The notation for the compset longname is
    TIME_ATM[%phys]_LND[%phys]_ICE[%phys]_OCN[%phys]_ROF[%phys]_GLC[%phys]_WAV[%phys][_ESP%phys][_BGC%phys]
    Where for the specific compsets below the following is supported
    TIME = Time period (e.g. 2000, HIST, RCP8...)
    ATM  = [CAM40, CAM50, CAM60]
    LND  = [CLM45, CLM50, SLND]
    ICE  = [CICE, DICE, SICE]
    OCN  = [DOCN, ,AQUAP, SOCN]
    ROF  = [RTM, MOSART, SROF]
    GLC  = [CISM1, CISM2, SGLC]
    WAV  = [WW3, DWAV, XWAV, SWAV]
    ESP  = [SESP]
    BGC  = optional BGC scenario

    The OPTIONAL %phys attributes specify submodes of the given system
    For example DOCN%DOM is the data ocean model for DOCN
    ALL the possible %phys choices for each component are listed.
    ALL data models must have a %phys option that corresponds to the data  model mode

    Each compset node is associated with the following elements
    - lname
    - alias
    - support  (optional description of the support level for this compset)
    Each compset node can also have the following attributes
    - grid  (optional regular expression match for grid to work with the compset)
  </help>

  <!-- nuopc testing -->
  <compset>
    <alias>S2S</alias>
    <lname>FCST_ufsatm%2017coupled_SLND_CICE_MOM6_SROF_SGLC_SWAV</lname>
  </compset>

  <entries>
    <entry id="RUN_STARTDATE">
      <values>
        <!-- default value for testing -->
        <value compset="FCST_ufsatm%2017">2016-10-03</value>
      </values>
    </entry>

    <entry id="START_TOD">
      <values>
        <!-- default value for testing -->
        <value compset="FCST_ufsatm%2017">0</value>
      </values>
    </entry>
  </entries>

</compsets>
